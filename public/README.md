# 背景画像の配置

このフォルダに背景画像を配置してください。

## 推奨ファイル名
- `cyber-city-bg.jpg` または `cyber-city-bg.png`

## 画像の要件
- 解像度: 1920x1080以上を推奨
- 形式: JPG, PNG, WebP
- サイズ: 可能な限り最適化（2MB以下を推奨）

## 画像が見つからない場合
画像ファイルが見つからない場合、デフォルトのグラデーション背景が表示されます。

